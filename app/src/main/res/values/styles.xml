<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/deepGray</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/deepGray</item>
    </style>

    <style name="Theme.Tab" parent="Widget.MaterialComponents.TabLayout">
        <item name="android:textColor">@color/whiteOpacity70</item>
    </style>

</resources>